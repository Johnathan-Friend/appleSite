// Johnathan Friend
"use strict";

//prints the page when button is clicked
const printButton = document.getElementById('print_button');
printButton.addEventListener('click', () => {
    print();
})